services:
  elasticsearch:
    image: elasticsearch:8.10.1
    container_name: elasticsearch
    environment:
      discovery.type: single-node
      ES_JAVA_OPTS: "-Xms2g -Xmx2g"
    volumes:
      - esdata:/usr/share/elasticsearch/data
      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
    ports:
      - "9200:9200"
      - "9300:9300"
    healthcheck:
      test: ["CMD-SHELL", "curl --silent --fail localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 3
    networks:
      - elastic
  logstash:
    image: logstash:8.10.1
    container_name: logstash
    environment:
      discovery.seed_hosts: logstash
      LS_JAVA_OPTS: "-Xms1g -Xmx1g"
    volumes:
      - ./logs:/usr/share/logstash/data/logs
      - ./aem-logs:/usr/share/logstash/data/aem-logs
      - ./logstash/config/pipelines.yml:/usr/share/logstash/config/pipelines.yml
      - ./logstash/pipeline/logstash-cdn-logs.config:/usr/share/logstash/pipeline/logstash-cdn-logs.config
      - ./logstash/pipeline/logstash-aem-logs.config:/usr/share/logstash/pipeline/logstash-aem-logs.config
    ports:
      - "6000:6000/tcp"
      - "6000:6000/udp"
      - "5044:5044"
      - "9600:9600"
    depends_on:
      - elasticsearch
    networks:
      - elastic
  kibana:
    image: kibana:8.10.1
    container_name: kibana
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
    networks:
      - elastic
    environment:
      ELASTICSEARCH_HOSTS: "http://elasticsearch:9200"
      INTERACTIVESETUP_ENABLED: false
    volumes:
      - kibanadata:/usr/share/kibana/data
      - ./kibana/config/kibana.yml:/usr/share/kibana/config/kibana.yml
networks:
  elastic:
    driver: bridge
volumes:
  esdata:
    driver: local
  kibanadata:
    driver: local
